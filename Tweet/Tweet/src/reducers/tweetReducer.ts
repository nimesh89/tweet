///<reference path="../../node_modules/redux/index.d.ts" />

export function tweetReducer(state = {
    timeline:[]
}, action) {
    switch (action.type) {
        default:
            return state;
    }
};